# polis-stroy

Новый сайт строительной компании ООО "Полис" (замена https://www.polis-stroy.ru/).

Страницы: О компании, Услуги, Каталог (12 категорий), Контакты.
Весь текущий контент и структура — в [content/site-content.md](content/site-content.md).

## Лендинг

Статичная одностраничная версия: все разделы (о компании, услуги, каталог, партнёры, контакты) на `docs/index.html` (папка, из которой публикует GitHub Pages) с якорной навигацией. Без сборки и зависимостей.

```
docs/index.html              разметка, SEO-метаданные, JSON-LD (GeneralContractor)
docs/assets/css/style.css    стили, mobile-first (брейкпоинты 48em и 68em)
docs/assets/js/main.js       меню, шапка, появление блоков, тема письма, копирование e-mail, телефонный док
docs/assets/fonts/           Playfair Display, Geologica, IBM Plex Mono — woff2, cyrillic + latin (OFL)
docs/assets/img/catalog/     картинки категорий со старого сайта (WebP)
docs/assets/img/og-image.jpg превью для соцсетей 1200×630
docs/favicon.svg, robots.txt, sitemap.xml
```

## Запуск

```sh
python3 -m http.server 8080 -d docs
# или: npx serve docs
```

Открыть http://localhost:8080/.

## Дизайн

- Палитра — токены в `:root` (`docs/assets/css/style.css`): графит `--ink`, кремовый `--paper`, единственный акцент `--accent` (лайм). Сменить акцент — одна переменная.
- Логотип (три башни с «ПОЛИС») перерисован из старого `logo.svg` в палитру сайта: SVG-символ `#mark` в `docs/index.html`, он же — чертёж на первом экране.
- Картинки категорий показаны в ч/б с `mix-blend-mode: multiply`, поэтому белый фон исходников растворяется в кремовом.
- Анимации отключаются при `prefers-reduced-motion`; без JS весь контент виден и ссылки работают.

## Телефон: док «Лифт + Заявка»

На экранах уже 48em вместо меню-бургера внизу, в зоне большого пальца, появляется док (включается скриптом, класс `html.is-handheld`):

- **Лифт** — этажи страницы ±0…05. Касание — плавный переезд на этаж. Ведёшь пальцем по доку — страница едет за пальцем, над ним табло «этаж 03 · Каталог», на каждом этаже короткая вибрация (Android). Активный этаж показывает, сколько раздела уже прочитано.
- **Заявка** — касание услуги или категории каталога (или свайп строки услуги вправо) кладёт позицию в смету, в док «прилетает» квадратик. Нижний лист (тянется пальцем вниз, чтобы закрыть) отправляет весь список одним письмом, копирует его для мессенджера или звонит. Блок «Тема обращения» в контактах тоже подхватывает заявку. Список живёт в `sessionStorage`.

На планшете и десктопе поведение прежнее.

## Что дальше

- Плитки каталога и строки услуг сейчас ведут к блоку контактов и подставляют тему в письмо (`mailto:`). Когда появятся страницы категорий, ссылки можно заменить на `/catalog/<slug>/`.
- Старые адреса `/services/`, `/catalog/`, `/contacts/` при переезде нужно либо сохранить отдельными страницами, либо настроить 301-редирект на якоря.
